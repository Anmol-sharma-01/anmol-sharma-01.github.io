services:
  portfolio:
    image: anmol-portfolio:local
    build: .
    ports:
      - '${HOST_BIND:-127.0.0.1}:${PORT:-8080}:8080'
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
